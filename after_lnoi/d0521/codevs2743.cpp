#include <iostream>
#include <cstdio>
#include <cstring>
#define pick(a, i) ((a >> (i - 1)) & 1)
int h, w;
int dp[13][3007];
int st[3007];

int main() {
    while (~scanf("%d %d", &h, &w)) {
        
    }
}